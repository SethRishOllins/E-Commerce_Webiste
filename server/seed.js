require('dotenv').config(); const mongoose = require('mongoose'); const bcrypt = require('bcryptjs'); const { Product, User } = require('./models'); const products = require('./catalog');
(async()=>{ await mongoose.connect(process.env.MONGODB_URI); await Product.deleteMany({}); await Product.insertMany(products); await User.findOneAndUpdate({email:'admin@novea.store'},{name:'Novea Admin',email:'admin@novea.store',password:await bcrypt.hash('Admin123!',12),role:'admin'},{upsert:true}); console.log('Seeded products and admin@novea.store / Admin123!'); await mongoose.disconnect(); })().catch(e=>{console.error(e);process.exit(1)});
